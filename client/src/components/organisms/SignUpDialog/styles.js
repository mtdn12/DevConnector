const styles = theme => ({
  contentWrapper: {
    marginTop: 60,
  },
  dialogAction: {
    padding: '0 10px 10px 0',
    margin: 0,
  },
  errorWrapper: {
    color: theme.palette.error.light,
  },
})

export default styles
